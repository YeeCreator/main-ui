# support advice (out-down): main-ui -> matheshop

## Context

`main-ui` supports `matheshop` because `matheshop` depends on `main-ui`.

## Adaptation Advice

Describe what `matheshop` should update if `main-ui` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
