# support advice (out-down): main-ui -> yeegames

## Context

`main-ui` supports `yeegames` because `yeegames` depends on `main-ui`.

## Adaptation Advice

Describe what `yeegames` should update if `main-ui` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
