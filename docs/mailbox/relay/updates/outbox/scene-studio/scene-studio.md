# support advice (updates-outbox): main-ui -> scene-studio

## Context

`main-ui` supports `scene-studio` because `scene-studio` depends on `main-ui`.

## Adaptation Advice

Describe what `scene-studio` should update if `main-ui` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
