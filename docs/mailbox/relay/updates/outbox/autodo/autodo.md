# support advice (out-down): main-ui -> autodo

## Context

`main-ui` supports `autodo` because `autodo` depends on `main-ui`.

## Adaptation Advice

Describe what `autodo` should update if `main-ui` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
