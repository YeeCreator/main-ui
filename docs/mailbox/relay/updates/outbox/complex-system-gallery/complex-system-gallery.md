# support advice (out-down): main-ui -> complex-system-gallery

## Context

`main-ui` supports `complex-system-gallery` because `complex-system-gallery` depends on `main-ui`.

## Adaptation Advice

Describe what `complex-system-gallery` should update if `main-ui` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
