/**
 * 数据层导出。
 *
 * 提供 TanStack Table 语义壳层与相关契约。
 */
export * from './DataTablePanel';
export type { ScreenAction, CopyField, SidebarControl, SidebarSection, SidebarModel } from '../types';
export type { DataTableAdapter, DataTableColumn } from '../adapters';
